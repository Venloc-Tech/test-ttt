import type { newt } from "~A/types/newt.ts";
import { version } from "@venloc-tech/package-b";
import type { Op } from "@venloc-tech/package-b";
import { o } from "~A/i/o.js";

export type x = newt;

export type {newt}

export type u = Op

export const y: u = {} as any;

export const a: x = "adjk"



console.log(version, a, y, o)